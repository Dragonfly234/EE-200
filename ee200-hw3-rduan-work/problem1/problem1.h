#ifndef PROBLEM1_H
#define PROBLEM1_H
#include <stdio.h>
int print_factors(int n);
int PrimeCheck(int n);// the int PrimeCheck(int n) function is used for checking whether number n(n>2) is a prime number.
                      //if n is a prime number , the function will return value 0, if not , it will return value 1.
		      //
#endif
